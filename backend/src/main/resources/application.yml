spring:

    datasource:
        url: jdbc:postgresql://localhost:5432/ewms
        username: postgres
        password: Manmohan@373

    flyway:
        enabled: true
        locations: classpath:db/migration

    jpa:
        hibernate:
            ddl-auto: validate

        open-in-view: false

        properties:
            hibernate:
                format_sql: true

    application:
        name: ewms-backend

server:
    port: 9999
